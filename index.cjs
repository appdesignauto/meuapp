const express=require('express'),fs=require('fs'),cors=require('cors'),app=express(),PORT=process.env.PORT||3000;app.use(cors());app.use(express.json());app.post('/api/webhook-hotmart',(req,res)=>{const log={timestamp:new Date().toISOString(),body:req.body};fs.appendFileSync('webhook-logs.json',JSON.stringify(log,null,2)+',\n');console.log('✅ Webhook recebido:',req.body);res.sendStatus(200);});app.listen(PORT,()=>console.log('🚀 Servidor rodando na porta',PORT));
