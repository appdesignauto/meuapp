/**
 * COMPONENTE DESATIVADO
 * 
 * Este componente foi completamente desativado, pois agora todos os emails são 
 * verificados automaticamente durante o processo de registro, fazendo com que 
 * este modal de verificação não seja mais necessário.
 * 
 * Mantido apenas como referência histórica no código - não exibe nenhuma UI.
 */

export function EmailVerificationModal() {
  // Retorna null para nunca mostrar o modal, pois a verificação agora é automática
  return null;
}